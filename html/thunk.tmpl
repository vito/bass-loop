<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width">
    <title>thunk {{.ThunkName}}</title>
    <link rel="stylesheet" type="text/css" href="/css/iosevka.css" />
    <link rel="stylesheet" type="text/css" href="/css/main.css" />
    <script type="text/javascript" src="/js/switcher.js"></script>
    <noscript>
      <link rel="stylesheet" type="text/css" href="/css/base16/base16-rose-pine.css" />
    </noscript>
  </head>
  <body>
    <div class="page">
      <div class="header">
        <span class="logo-and-text">
          {{.Avatar}}
          <div class="heading-and-name">
            <h1>thunk</h1>
            <ul>
              <li><strong>thunk</strong> <a class="name" href="/thunks/{{.ThunkName}}">{{.ThunkName}}</a></li>
            </ul>
          </div>
        </span>
      </div>

      <h2>runs</h2>

      <ul class="thunk-runs">
      {{range .Runs}}
        <li><a class="name" href="/runs/{{.Run.ID}}">{{.Run.ID}}</a> {{.Duration}}</li>
      {{end}}
      </ul>

      <h2>json</h2>

      <div class="highlight" contenteditable="true">
        {{.JSON}}
      </div>

      <div class="footer">
        <div class="choose-theme" id="choosetheme">
          <select id="styleswitcher" onchange="switchStyle(event)">
            {{template "base16-options.tmpl"}}
          </select>
        </div>
      </div>
    </div>
  </body>
</html>
